import 'package:flutter/material.dart';

class ThemeConstant {
  // Primary Color (Cyan for the main theme)
  static const Color primaryColor =
      Color.fromARGB(255, 39, 133, 192); // Cyan color.

  // Dark Primary Color (Darker Cyan for dark mode)
  static const Color darkPrimaryColor = Colors.cyan; // Dark cyan shade.

  // Accent Color (Golden yellow for highlights)
  static const Color accentColor =
      Color(0xFFFFD700); // Bright gold for a football vibe.

  // AppBar Color (Dark for contrast)
  static const Color appBarColor = Colors.cyan; // Dark app bar.
}
